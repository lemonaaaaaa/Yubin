/**
 * ajax2.js
 */
 
 
 let promise = new promise(function (resolve, rejet){
 }      )
 val -= 1;
 if (val> 0) {
 resolve ( 'succecs,  ' + cal);
 }else {
 reject('error');
 }
 });
 
 promise.them((result1 =  {
 return new Promise((nesolve, requet){
 		val -= 2
 		result1('success',val);
 		}else{
 		regerft2('error''')}
 })